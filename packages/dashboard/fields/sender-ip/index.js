/**
 * WordPress dependencies.
 */
import { __ } from '@wordpress/i18n';

const senderIpField = {
	id: 'omniform_form.sender_ip',
	label: __( 'Sender IP', 'omniform' ),
};

export default senderIpField;
